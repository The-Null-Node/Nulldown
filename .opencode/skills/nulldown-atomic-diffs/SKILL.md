---
name: nulldown-atomic-diffs
description: Applies authorized Nulldown-hosted document, plan, checklist, strategy, and memory changes as focused atomic branch diffs with verification.
---

# Nulldown Atomic Diffs

Hosted writes are opt-in. Read the current branch state before editing, apply
one focused `diff_apply` event with meaningful metadata, then verify through a
resolved query. Use direct MCP tools when available; use `nd diff apply` only
for a selected CLI workflow or missing MCP capability.

Never replace a whole root when a branch diff is intended. Record facts or
procedures only when those record classes are explicitly authorized.

## Hosted Strategy

Canonical strategy program: https://nulldown.app/d/VfrzR4

Load root `VfrzR4ydPA05` with `strategy_get` before applying this skill. Atomic
write mechanics remain subordinate to the selected project strategy.
