---
name: nulldown-priority-strategy
description: Retrieves current hosted Nulldown priorities and child-plan constraints before architecture, refactor, planning, strategy, skill, or agent-behavior work in the Nulldown repository.
---

# Nulldown Priority Strategy

Current user direction selects the work. Hosted plans constrain that selected
work; they do not authorize implementation or nominate themselves.

Before planning, query master root `1wrhjx8Wzk67`, current account-owned branch
`clone_account:2d89645f-f4b5-40be-a890-9e279ff4c46b`, for the task terms. Query
the relevant child plan before editing. Prefer direct MCP reads; never resolve a
branch merely to inspect strategy.

The explicit branch is a compatibility fallback until the master root owns its
current branch through `metadata.strategyRef`.

## Hosted Strategy

Canonical strategy: https://nulldown.app/d/1wrhjx

Use the stable root as the association. Follow its drop-owned
`metadata.strategyRef` once populated; do not pin snapshots in this skill.
